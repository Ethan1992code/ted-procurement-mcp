# 网页购买进度

2026-09-22：新增 /shop 中文页面预览，显示1.99元单次价格、最多25条、历史公告和空结果计费说明。支付按钮禁用，不调用原Agent付款接口。

更新：已实现网页下单与支付宝表单、独立网页订单迁移、同浏览器订单访问、异步通知、交易查询补偿和检索结果持久化。WEBPAY_ENABLED 未启用，仍显示预览。退款与关单管理尚未实现，真实网页付款、通知和手机回跳尚未联调；不能称为完整支付集成或生产就绪。

33项Python相关测试通过；网页订单迁移在本地PGlite PostgreSQL验证重复执行、访问隔离、幂等创建、支付前禁止交付、金额校验、租约和结果复用。用户报告网页收款已开通，并选择现有TED应用；尚未通过生产支付验证该权限。

下一步先在新Supabase项目运行schema/webpay.sql。另需设置WEBPAY_BASE_URL=https://ted-procurement-mcp.vercel.app，并在完成剩余验证后启用WEBPAY_ENABLED。网页使用现有同应用的ALIPAY配置，价格必须为1.99；不得展示密钥。

接入产品为AI网页应用收款：alipay.trade.page.pay / FAST_INSTANT_TRADE_PAY，使用已安装官方Python SDK的page_execute，浏览器渲染并提交表单。通知和交易查询通过后才标记付款成功，同步回跳不能作为成功依据。

上线前需要确认个人账号的网页收款权限与应用授权。原AI按量付费的开通状态不能替代网页收款资格。沙箱网页支付尚未验证；真实付款不得自动发起。

后续实现：独立网页订单与访问令牌、金额固定且服务端定价、订单写入后生成支付表单、通知验签和归属校验、幂等状态更新、支付查询兜底、结果持久化、退款请求持久化与查询、关闭未付订单、真实浏览器和支付联调。
